apiVersion: v1
kind: Secret
metadata:
  name: {{ include "qc-app.fullname" . | trunc 63 | trimSuffix "-" }}-secrets
type: Opaque
stringData:
  # Fill at deploy time via Helm values or external secrets
  JWT_SECRET_KEY: ""
  DB_PASSWORD: ""
