apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "qc-app.fullname" . | trunc 63 | trimSuffix "-" }}-config
data:
  {{- range $name, $value := .Values.env }}
  {{ $name }}: {{ $value | quote }}
  {{- end }}
